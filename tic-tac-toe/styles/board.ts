import { StyleSheet } from "react-native";

export const board = StyleSheet.create({
    row: {
        flexDirection: "row",
    },

    text: {
        fontSize: 45,
        marginBottom: 30,
    },
})