import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    square: {
        width: 40,
        height: 40,
        borderWidth: 1,
        justifyContent: "center",
        alignItems: "center",
    },

    text: {
        fontSize: 30,
        textAlign: "center",
    },

    row: {
        flexDirection: "row",
    },
})