import { StyleSheet } from "react-native";

export const board = StyleSheet.create({
  row: {
    flexDirection: "row",
  },
  
  text: {
    fontSize: 24,
    fontWeight: "600",
    marginBottom: 20,
    color: "#111827",
    textAlign: "center",
  },
});
