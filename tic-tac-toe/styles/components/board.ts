import { StyleSheet } from "react-native";

export const board = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    paddingTop: 10,
    paddingBottom: 30,
    left: 10,
  },
  row: {
    flexDirection: "row",
  },
});
