import { StyleSheet } from "react-native";

export const board = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    alignSelf: "center",
    paddingTop: 5,
    paddingBottom: 5,
  },
  row: {
    flexDirection: "row",
  },
});